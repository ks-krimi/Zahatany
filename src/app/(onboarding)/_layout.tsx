import { Stack } from "expo-router";

const OnboardingRootLayout = () => {
  return (
    <Stack
      screenOptions={{
        headerShown: false,
      }}
    />
  );
};

export default OnboardingRootLayout;
